import { createConfig } from '@gluestack-ui/config';

export const theme = createConfig({
  colors: {
    primary: '#ffcc00',
    secondary: '#333',
  },
  fontSizes: {
    sm: 12,
    md: 16,
    lg: 20,
  },
});
