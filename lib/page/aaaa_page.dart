import 'package:flutter/material.dart';

class AaaaPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) => Scaffold(
    backgroundColor: Colors.red,
    body: Center(
      child: Text(
        'Tome',
        style: TextStyle(fontSize: 60, color: Colors.white),
      ),
    ),
  );
}